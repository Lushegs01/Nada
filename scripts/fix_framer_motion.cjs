const fs=require('fs');const p=require('path');function walk(d){fs.readdirSync(d).forEach(f=>{const pth=p.join(d,f);if(fs.statSync(pth).isDirectory())walk(pth);else if(pth.endsWith('.tsx')||pth.endsWith('.ts')){let c=fs.readFileSync(pth,'utf8');if((c.includes('motion')||c.includes('AnimatePresence')) && !c.includes('"framer-motion"')){fs.writeFileSync(pth,'import { motion, AnimatePresence } from "framer-motion";\n'+c);}}});}walk('apps/web/src/components');
